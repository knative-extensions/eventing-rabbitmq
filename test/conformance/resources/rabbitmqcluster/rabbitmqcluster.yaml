apiVersion: rabbitmq.com/v1beta1
kind: RabbitmqCluster
metadata:
  name: {{ .name }}
  namespace: {{ .namespace }}
spec:
  replicas: 1
  resources:
    limits:
      cpu: "400m"
      memory: "850Mi"
    requests:
      cpu: "400m"
      memory: "850Mi"
